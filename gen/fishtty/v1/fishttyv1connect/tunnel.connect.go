// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: fishtty/v1/tunnel.proto

package fishttyv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/frozenf1sh/fishpts/gen/fishtty/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// FishTTYName is the fully-qualified name of the FishTTY service.
	FishTTYName = "fishtty.v1.FishTTY"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// FishTTYTunnelProcedure is the fully-qualified name of the FishTTY's Tunnel RPC.
	FishTTYTunnelProcedure = "/fishtty.v1.FishTTY/Tunnel"
)

// FishTTYClient is a client for the fishtty.v1.FishTTY service.
type FishTTYClient interface {
	// Tunnel 是唯一的 RPC：Agent 调用它建立双向流隧道。
	// 流上承载 TunnelMessage，按 session_id 路由到不同 PTY 会话。
	Tunnel(context.Context) *connect.BidiStreamForClient[v1.TunnelMessage, v1.TunnelMessage]
}

// NewFishTTYClient constructs a client for the fishtty.v1.FishTTY service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFishTTYClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FishTTYClient {
	baseURL = strings.TrimRight(baseURL, "/")
	fishTTYMethods := v1.File_fishtty_v1_tunnel_proto.Services().ByName("FishTTY").Methods()
	return &fishTTYClient{
		tunnel: connect.NewClient[v1.TunnelMessage, v1.TunnelMessage](
			httpClient,
			baseURL+FishTTYTunnelProcedure,
			connect.WithSchema(fishTTYMethods.ByName("Tunnel")),
			connect.WithClientOptions(opts...),
		),
	}
}

// fishTTYClient implements FishTTYClient.
type fishTTYClient struct {
	tunnel *connect.Client[v1.TunnelMessage, v1.TunnelMessage]
}

// Tunnel calls fishtty.v1.FishTTY.Tunnel.
func (c *fishTTYClient) Tunnel(ctx context.Context) *connect.BidiStreamForClient[v1.TunnelMessage, v1.TunnelMessage] {
	return c.tunnel.CallBidiStream(ctx)
}

// FishTTYHandler is an implementation of the fishtty.v1.FishTTY service.
type FishTTYHandler interface {
	// Tunnel 是唯一的 RPC：Agent 调用它建立双向流隧道。
	// 流上承载 TunnelMessage，按 session_id 路由到不同 PTY 会话。
	Tunnel(context.Context, *connect.BidiStream[v1.TunnelMessage, v1.TunnelMessage]) error
}

// NewFishTTYHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFishTTYHandler(svc FishTTYHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	fishTTYMethods := v1.File_fishtty_v1_tunnel_proto.Services().ByName("FishTTY").Methods()
	fishTTYTunnelHandler := connect.NewBidiStreamHandler(
		FishTTYTunnelProcedure,
		svc.Tunnel,
		connect.WithSchema(fishTTYMethods.ByName("Tunnel")),
		connect.WithHandlerOptions(opts...),
	)
	return "/fishtty.v1.FishTTY/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FishTTYTunnelProcedure:
			fishTTYTunnelHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFishTTYHandler returns CodeUnimplemented from all methods.
type UnimplementedFishTTYHandler struct{}

func (UnimplementedFishTTYHandler) Tunnel(context.Context, *connect.BidiStream[v1.TunnelMessage, v1.TunnelMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("fishtty.v1.FishTTY.Tunnel is not implemented"))
}
